export const orgKey = (orgId: string): string => `ORG#${orgId}`;
export const skuKey = (sku: string): string => `SKU#${sku}`;
export const sessionKey = (sessionId: string): string => `SESSION#${sessionId}`;
export const objectKey = (objectId: string): string => `OBJECT#${objectId}`;
export const eventKey = (eventId: string): string => `EVENT#${eventId}`;
export const taskKey = (taskId: string): string => `TASK#${taskId}`;
